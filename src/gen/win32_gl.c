void *gl_get_proc_adress(const char *name);
#define GL_GETPROC(name) name = (gl_##name *)gl_get_proc_adress(#name)
gl_glClearColor *glClearColor;
gl_glClear *glClear;
gl_glCreateShader *glCreateShader;
gl_glCompileShader *glCompileShader;
gl_glDeleteShader *glDeleteShader;
gl_glGetShaderInfoLog *glGetShaderInfoLog;
gl_glGetShaderiv *glGetShaderiv;
gl_glCreateProgram *glCreateProgram;
gl_glShaderSource *glShaderSource;
gl_glLinkProgram *glLinkProgram;
gl_glDeleteProgram *glDeleteProgram;
gl_glAttachShader *glAttachShader;
gl_glGetProgramiv *glGetProgramiv;
gl_glGetProgramInfoLog *glGetProgramInfoLog;
gl_glUseProgram *glUseProgram;
gl_glDrawArrays *glDrawArrays;
gl_glDrawElements *glDrawElements;
gl_glGenVertexArrays *glGenVertexArrays;
gl_glBindVertexArray *glBindVertexArray;
gl_glVertexAttrib1f *glVertexAttrib1f;
gl_glGetString *glGetString;
gl_glGetIntegerv *glGetIntegerv;
gl_glGenBuffers *glGenBuffers;
gl_glBindBuffer *glBindBuffer;
gl_glBufferData *glBufferData;
gl_glBufferSubData *glBufferSubData;
gl_glVertexAttribPointer *glVertexAttribPointer;
gl_glEnableVertexAttribArray *glEnableVertexAttribArray;
gl_glDisableVertexAttribArray *glDisableVertexAttribArray;
gl_glEnable *glEnable;
gl_glGetUniformLocation *glGetUniformLocation;
gl_glUniformMatrix4fv *glUniformMatrix4fv;
gl_glUniform3fv *glUniform3fv;
gl_glUniform1i *glUniform1i;
gl_glGenTextures *glGenTextures;
gl_glActiveTexture *glActiveTexture;
gl_glBindTexture *glBindTexture;
gl_glTexImage2D *glTexImage2D;
gl_glTexParameteri *glTexParameteri;
gl_glPixelStorei *glPixelStorei;
gl_glBlendFunc *glBlendFunc;
gl_glGenerateMipmap *glGenerateMipmap;
gl_glViewport *glViewport;

static void platform_win32_gl_init_function_pointers(void)
{
    GL_GETPROC(glClearColor);
    GL_GETPROC(glClear);
    GL_GETPROC(glCreateShader);
    GL_GETPROC(glCompileShader);
    GL_GETPROC(glDeleteShader);
    GL_GETPROC(glGetShaderInfoLog);
    GL_GETPROC(glGetShaderiv);
    GL_GETPROC(glCreateProgram);
    GL_GETPROC(glShaderSource);
    GL_GETPROC(glLinkProgram);
    GL_GETPROC(glDeleteProgram);
    GL_GETPROC(glAttachShader);
    GL_GETPROC(glGetProgramiv);
    GL_GETPROC(glGetProgramInfoLog);
    GL_GETPROC(glUseProgram);
    GL_GETPROC(glDrawArrays);
    GL_GETPROC(glDrawElements);
    GL_GETPROC(glGenVertexArrays);
    GL_GETPROC(glBindVertexArray);
    GL_GETPROC(glVertexAttrib1f);
    GL_GETPROC(glGetString);
    GL_GETPROC(glGetIntegerv);
    GL_GETPROC(glGenBuffers);
    GL_GETPROC(glBindBuffer);
    GL_GETPROC(glBufferData);
    GL_GETPROC(glBufferSubData);
    GL_GETPROC(glVertexAttribPointer);
    GL_GETPROC(glEnableVertexAttribArray);
    GL_GETPROC(glDisableVertexAttribArray);
    GL_GETPROC(glEnable);
    GL_GETPROC(glGetUniformLocation);
    GL_GETPROC(glUniformMatrix4fv);
    GL_GETPROC(glUniform3fv);
    GL_GETPROC(glUniform1i);
    GL_GETPROC(glGenTextures);
    GL_GETPROC(glActiveTexture);
    GL_GETPROC(glBindTexture);
    GL_GETPROC(glTexImage2D);
    GL_GETPROC(glTexParameteri);
    GL_GETPROC(glPixelStorei);
    GL_GETPROC(glBlendFunc);
    GL_GETPROC(glGenerateMipmap);
    GL_GETPROC(glViewport);

    assert(glClearColor);
    assert(glClear);
    assert(glCreateShader);
    assert(glCompileShader);
    assert(glDeleteShader);
    assert(glGetShaderInfoLog);
    assert(glGetShaderiv);
    assert(glCreateProgram);
    assert(glShaderSource);
    assert(glLinkProgram);
    assert(glDeleteProgram);
    assert(glAttachShader);
    assert(glGetProgramiv);
    assert(glGetProgramInfoLog);
    assert(glUseProgram);
    assert(glDrawArrays);
    assert(glDrawElements);
    assert(glGenVertexArrays);
    assert(glBindVertexArray);
    assert(glVertexAttrib1f);
    assert(glGetString);
    assert(glGetIntegerv);
    assert(glGenBuffers);
    assert(glBindBuffer);
    assert(glBufferData);
    assert(glBufferSubData);
    assert(glVertexAttribPointer);
    assert(glEnableVertexAttribArray);
    assert(glDisableVertexAttribArray);
    assert(glEnable);
    assert(glGetUniformLocation);
    assert(glUniformMatrix4fv);
    assert(glUniform3fv);
    assert(glUniform1i);
    assert(glGenTextures);
    assert(glActiveTexture);
    assert(glBindTexture);
    assert(glTexImage2D);
    assert(glTexParameteri);
    assert(glPixelStorei);
    assert(glBlendFunc);
    assert(glGenerateMipmap);
    assert(glViewport);
}
