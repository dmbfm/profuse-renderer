#include "allocator.h"

//Result(uptr) allocator_alloc(Allocator *allocator, usize amount)
//{
//    return allocator->alloc(allocator, amount, ALLOCATOR_MAX_ALIGNMENT);
//}
//
//void allocator_free(Allocator *allocator, uptr region)
//{
//    allocator->resize(allocator, region, 0, ALLOCATOR_MAX_ALIGNMENT);
//}
//
